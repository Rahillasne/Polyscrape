/**
 * Site-wide configuration.
 *
 * GITHUB_REPO drives the "Report wrong date" links, which open a prefilled
 * GitHub issue. Change this placeholder to your own "owner/repo" after forking
 * (the README explains how).
 */
export const GITHUB_REPO = "Rahillasne/Polyscrape";

/** Human-facing site title, used in the document <title> and the hero. */
export const SITE_TITLE = "FundingDeadlines";

/** One-line subtitle shown under the hero title. */
export const SITE_SUBTITLE =
  "Deadlines for accelerators, pitch competitions, grants, and fellowships — in one calm place.";
